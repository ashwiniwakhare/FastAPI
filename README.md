# FastAPI Blog API

A secure and scalable Blog REST API built using **FastAPI**, **JWT Authentication**, and **SQLAlchemy ORM**.

## 🚀 Features
- User Register & Login with JWT Token
- CRUD operations for Blogs
- Secured routes using OAuth2 + JWT
- Password Hashing using Passlib (bcrypt)
- SQLAlchemy ORM with MySQL/PostgreSQL/SQLite support

## 📌 Tech Stack
FastAPI | SQLAlchemy | JWT | OAuth2 | Uvicorn | Passlib
